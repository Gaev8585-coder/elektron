name: Manual Data Update

on:
  workflow_dispatch:
    inputs:
      temperature:
        description: 'Температура'
        required: true
        default: '22.9'
      humidity:
        description: 'Влажность'
        required: true  
        default: '40.2'
      voltage:
        description: 'Напряжение'
        required: true
        default: '12.34'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Update data
        run: |
          echo '{
            "temperature": ${{ github.event.inputs.temperature }},
            "humidity": ${{ github.event.inputs.humidity }},
            "voltage": ${{ github.event.inputs.voltage }},
            "lastUpdate": "$(date -u +"%Y-%m-%dT%H:%M:%S.%3NZ")",
            "status": "success"
          }' > docs/data.json
          
      - name: Commit
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/data.json
          git commit -m "🤖 Ручное обновление: ${{ github.event.inputs.temperature }}°C"
          git push
