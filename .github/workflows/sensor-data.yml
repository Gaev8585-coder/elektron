name: Sensor Data Update

on:
  repository_dispatch:
    types: [sensor-data]

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Debug step
        run: |
          echo "✅ Workflow запущен"
          echo "Данные от ESP8266:"
          echo '${{ toJson(github.event.client_payload) }}'
      
      - name: Simple update
        run: |
          # Простое обновление для теста
          echo '{"temperature": 19.9, "humidity": 42.9, "voltage": 3.95, "status": "success"}' > docs/data.json
          
      - name: Commit changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/data.json
          git commit -m "🤖 Авто-обновление от ESP8266"
          git push
