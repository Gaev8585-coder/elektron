name: Update Sensor Data

on:
  # Разрешаем ручной запуск
  workflow_dispatch:
  # И запуск через API от ESP8266
  repository_dispatch:
    types: [sensor-data]

jobs:
  update-sensor-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Debug received data
        run: |
          echo "📊 Получены данные:"
          echo '${{ toJson(github.event.client_payload) }}'
          
      - name: Update data.json
        run: |
          # Создаем обновленный data.json
          cat > docs/data.json << 'EOF'
          {
            "temperature": ${{ github.event.client_payload.temperature || 23.5 }},
            "humidity": ${{ github.event.client_payload.humidity || 45.0 }},
            "voltage": ${{ github.event.client_payload.voltage || 12.34 }},
            "lastUpdate": "$(date -u +"%Y-%m-%dT%H:%M:%S.%3NZ")",
            "status": "success"
          }
          EOF
          
      - name: Show updated file
        run: |
          echo "📄 Содержимое data.json:"
          cat docs/data.json
          
      - name: Commit and push changes
        run: |
          git config --local user.name "GitHub Action"
          git config --local user.email "action@github.com"
          git add docs/data.json
          git commit -m "🤖 Auto-update sensor data" || exit 0
          git push
